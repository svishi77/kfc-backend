services:
  db_backend_kffc:
    image: db_backend_kffc
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: "python main.py"
    ports:
      - "3000:8000"
    restart: always
